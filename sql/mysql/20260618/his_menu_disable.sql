-- ====================================================================
-- HIS 系统菜单禁用脚本
-- 禁用非 HIS 核心业务模块的菜单
-- 执行时间: 2026-06-18
-- 说明: 关闭 CRM、ERP、MES、WMS、IoT、商城系统等模块菜单
-- ====================================================================

-- 使用数据库
USE `yudao-ai-his`;

-- ====================================================================
-- 1. 禁用 CRM 系统菜单 (id=2397)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 2397  -- CRM 系统 (目录)
   OR `parent_id` = 2397  -- CRM 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 2397
   );  -- CRM 二级子菜单

-- ====================================================================
-- 2. 禁用 ERP 系统菜单 (id=2563)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 2563  -- ERP 系统 (目录)
   OR `parent_id` = 2563  -- ERP 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 2563
   );  -- ERP 二级子菜单

-- ====================================================================
-- 3. 禁用 MES 系统菜单 (id=5100)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 5100  -- MES 系统 (目录)
   OR `parent_id` = 5100  -- MES 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 5100
   );  -- MES 二级子菜单

-- ====================================================================
-- 4. 禁用 WMS 系统菜单 (id=6400)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 6400  -- WMS 系统 (目录)
   OR `parent_id` = 6400  -- WMS 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 6400
   );  -- WMS 二级子菜单

-- ====================================================================
-- 5. 禁用 IoT 物联网菜单 (id=4000)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 4000  -- IoT 物联网 (目录)
   OR `parent_id` = 4000  -- IoT 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 4000
   );  -- IoT 二级子菜单

-- ====================================================================
-- 6. 禁用商城系统菜单 (id=2362)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 2362  -- 商城系统 (目录)
   OR `parent_id` = 2362  -- 商城直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 2362
   );  -- 商城二级子菜单

-- ====================================================================
-- 7. 禁用会员中心菜单 (id=2262) - 商城相关
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 2262  -- 会员中心 (目录)
   OR `parent_id` = 2262  -- 会员中心直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 2262
   );  -- 会员中心二级子菜单

-- ====================================================================
-- 8. 禁用公众号管理菜单 (id=2084) - 非医院必需
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 2084  -- 公众号管理 (目录)
   OR `parent_id` = 2084  -- 公众号直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 2084
   );  -- 公众号二级子菜单

-- ====================================================================
-- 9. 禁用 AI 大模型菜单 (id=2758) - 非医院核心功能
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 2758  -- AI 大模型 (目录)
   OR `parent_id` = 2758  -- AI 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 2758
   );  -- AI 二级子菜单

-- ====================================================================
-- 10. 禁用支付管理菜单 (id=1117) - HIS 使用独立支付系统
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 1117  -- 支付管理 (目录)
   OR `parent_id` = 1117  -- 支付直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 1117
   );  -- 支付二级子菜单

-- ====================================================================
-- 11. 禁用工作流程菜单 (id=1185) - BPM 流程
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 1185  -- 工作流程 (目录)
   OR `parent_id` = 1185  -- 工作流程直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 1185
   );  -- 工作流程二级子菜单

-- ====================================================================
-- 12. 禁用报表管理菜单 (id=1281) - 非医院核心
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 1281  -- 报表管理 (菜单)
   OR `parent_id` = 1281;  -- 报表子菜单

-- ====================================================================
-- 13. 禁用代码生成案例菜单 (id=1070)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 1070  -- 代码生成案例 (目录)
   OR `parent_id` = 1070;  -- 案例子菜单

-- ====================================================================
-- 14. 禁用 IM 即时通讯菜单 (id=6500) - 非医院核心功能
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` = 6500  -- IM 即时通讯 (目录)
   OR `parent_id` = 6500  -- IM 直接子菜单
   OR `parent_id` IN (
       SELECT `id` FROM `system_menu` WHERE `parent_id` = 6500
   );  -- IM 二级子菜单

-- ====================================================================
-- 15. 禁用开发者文档菜单 (外部链接)
-- ====================================================================
UPDATE `system_menu`
SET `status` = 1
WHERE `id` IN (1254, 2159, 2160);  -- 作者动态、Boot文档、Cloud文档

-- ====================================================================
-- 查询统计
-- ====================================================================
SELECT
    '已禁用菜单统计' AS '说明',
    COUNT(*) AS '禁用数量'
FROM `system_menu`
WHERE `status` = 1 AND `deleted` = 0;

-- 查看剩余激活的顶级菜单
SELECT
    id, name, type, path, status
FROM `system_menu`
WHERE `parent_id` = 0
  AND `deleted` = 0
  AND `status` = 0
ORDER BY id;

-- ====================================================================
-- 完成
-- ====================================================================
-- 提示: 执行后需要清除菜单缓存
-- Redis 命令: DEL permission_menu_ids
-- ====================================================================